<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #2a2b33;
        }
        .form-wrapper {
            width: 100%;
            display: flex;
            justify-content: center;
        }
        form {
            border-style: groove;
            margin-top: -100px;
            width: 550px;
            min-height: 300px;
            display: flex;
            flex-direction: column;
            padding: 20px;
            border-color: #ffffff;
        }
        .form-header {
            width: 100%;
            height: 200px;
            padding-top: 20;
            text-align: center;
            font-size: 20;
            color: #ffffff;
            font-family: "Lucida Console", "Courier New", monospace;
        }
        input {
            padding: 10px;
            background-color: rgba(255, 255, 255, 0.25);
            border: none;
            color: #ffffff;
        }
        .submit {
            padding: 10px;
            margin-top: 20px;
            background-color: #15478d;
            color: #ffffff;
        }
    </style>
    <%- include('../partials/header'); %>
</head>
<body>
    <div class="form-wrapper">
        <form action="/choose" method="post">
            <label class="form-header">Enter email of the person:</label>
            <input type="email" name="email" value="<%= email%>">
            <button class="submit">ENTER</button>
        </form>
    </div>
</body>
</html>